import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        padding: 32
    },
    subTitle: {
        color: '#000',
        fontSize: 20,
        marginTop: 40,
        marginBottom: 18,
        fontWeight: 'bold'
    }
});


export default styles;
